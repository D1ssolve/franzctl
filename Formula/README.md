# Homebrew tap

GoReleaser generates the current `franzctl` Cask from `.goreleaser.yaml` and publishes it
to the configured `homebrew-tap` repository. A static formula is intentionally
not committed here because release URLs and SHA-256 checksums must come from
the actual tagged release.
